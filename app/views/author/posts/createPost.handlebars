<div class="row mbn-20">

    <!--Form Field-->
    <div class="col-lg-12 col-12 mb-20">
        <div class="box">
            <div class="box-head">
                <h6 class="mb-15">ایجاد مطالب جدید</h6>
            </div>
            <div class="box-body">

                {{#each errors}}
                    <div class="alert alert-outline-danger" role="alert">
                        <i class="zmdi zmdi-info"></i>
                        <span>{{this}}</span>
                    </div>
                {{/each}}
                <form action="/admin/posts/store" method="post">
                    <div class="row mbn-15">
                        <div class="col-8 mb-15">
                            <input
                                name="title"
                                type="text"
                                class="form-control"
                                placeholder="عنوان مطلب"
                            />
                        </div>
                        <div class="col-4 mb-15">
                            <select class="form-control" name="author_id">
                                <option disabled selected>انتخاب</option>
                                {{#each users}}
                                    <option
                                        value="{{this.id}}"
                                    >{{this.full_name}}</option>
                                {{/each}}
                            </select>
                        </div>

                        <div class="col-8 mb-15">
                            <textarea
                                name="content"
                                class="form-control summernote"
                                placeholder="محتوای مطلب"
                            ></textarea>
                        </div>
                        <div class="col-lg-4 col-4 mb-20">

                            <h6 class="mb-15">وضعیت مطلب</h6>

                            <label><input
                                    type="radio"
                                    name="status"
                                    value="0"
                                    checked
                                />پیشنویس</label>
                            <label><input
                                    type="radio"
                                    name="status"
                                    value="1"
                                />در حال بازبینی</label>
                            <label><input
                                    type="radio"
                                    name="status"
                                    value="2"
                                />منتشر شده</label>

                        </div>
                        <div class="col-12 mb-15 d-flex justify-content-end">
                            <button
                                type="submit"
                                class="button button-primary button-clipboard mb-0 mt-15"
                            >ثبت</button>
                        </div>

                    </div>
                </form>
            </div>
        </div>

    </div>
    <!--Form Field-->

</div>